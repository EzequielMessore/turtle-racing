name: pipeline check

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main

jobs:
  ktlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Ktlint Verification
        run: ./gradlew ktlint
  unit_tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Unit tests
        run: ./gradlew test
